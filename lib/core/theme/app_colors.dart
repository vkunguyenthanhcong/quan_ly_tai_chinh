import 'package:flutter/material.dart';

class AppColors {
  // ===== BRAND =====
  static const primary = Color(0xFF3B82F6); // xanh nút +
  static const secondary = Color(0xFF22D3EE); // chart xanh ngọc
  static const accent = Color(0xFFF97316); // chart cam

  static const success = Color(0xFF22C55E);
  static const danger = Color(0xFFEF4444);

  // ===== DARK =====
  static const darkBg = Color(0xFF0F172A); // nền chính
  static const darkCard = Color(0xFF1E293B); // card
  static const darkCardSoft = Color(0xFF334155);

  static const darkText = Colors.white;
  static const darkSubText = Color(0xFFCBD5E1);
  static const darkDivider = Color(0xFF334155);

  // ===== LIGHT =====
  static const lightBg = Color(0xFFF8FAFC);
  static const lightCard = Colors.white;
  static const lightCardSoft = Color(0xFFF1F5F9);

  static const lightText = Color(0xFF0F172A);
  static const lightSubText = Color(0xFF475569);
  static const lightDivider = Color(0xFFE2E8F0);

  // ===== CHART =====
  static const chartBlue = Color(0xFF22D3EE);
  static const chartPurple = Color(0xFFA78BFA);
  static const chartPink = Color(0xFFF472B6);
  static const chartOrange = Color(0xFFFB923C);
}
